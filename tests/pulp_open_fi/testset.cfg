from gvtest.testsuite import *

def testset_build(testset):
	testset.set_name('pulp_open_fi')

	cwd = testset.get_path()

	test = testset.new_test("deterministic_fi")
	test.add_command(Shell(
		"run",
		f"make -C {cwd} fcm"
	))
