from gvtest.testsuite import *


def check_output(test, output):
    if output.find('Hello from ri5ky_testbench') == -1:
        return (False, "Didn't find hello string in output\n")
    return (True, None)


def testset_build(testset):
    testset.new_gvrun_test('hello', checker=check_output)
