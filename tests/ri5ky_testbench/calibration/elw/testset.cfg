from gvtest.testsuite import *

# p.elw against the asynchronous slow memory (latency=5). RI5CY fully
# serializes the elw in ELW_EXE: it pays the response latency plus a
# 1-cycle pipeline refill, with no overlap. Per elw:
#   total  = L + 2 = 7 cycles      (RTL 449 / 64 loads)
#   PCCR_ELW = L + 1 = 6 cycles    (RTL 384 = perf_pipeline_stall_o)
# Both match RTL exactly; GVSoC clock-gates the core for the wait and
# charges the same wasted cycles to CSR_PCER_ELW.
# variant -> (RTL reference cycles, absolute tolerance)
REFS = {
    'elw_fastmode': (7 * 64, 15),
    'elw_slowmode': (7 * 64, 15),
}

# PCER cross-check: one bus access per elw (ld == 64, exact) and the ELW
# counter charges L+1 = 6 wasted cycles per elw (~384, ±8).
# metric -> (PCER field, ref, tol)
PCER = {
    'pcer.ld': ('ld', 64, 0),
    'pcer.elw': ('elw', 6 * 64, 8),
}


def testset_build(testset):
    test = testset.new_gvrun_test('elw')
    for variant, (ref, tol) in REFS.items():
        test.add_bench(f'cycles.{variant.removeprefix("elw_")}',
                       rf'CALIB {variant} cycles=(\d+)',
                       f'{variant} (cycles)', ref=ref, tol=tol, ref_type='rtl')
    for metric, (field, ref, tol) in PCER.items():
        test.add_bench(metric,
                       rf'CALIB_PCER elw .*\b{field}=(\d+)',
                       f'elw {field}', ref=ref, tol=tol, ref_type='rtl')
